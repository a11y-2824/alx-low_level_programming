#include "main.h"

/**
 * int _islower(int c) - checks for lowercase character
 * 
 * Description: Returns 1 if c is lowercase
 * Return: 0 or 1
 */
int _islower(int c)
{
	return (c >= 97 && c <= 122);

}
